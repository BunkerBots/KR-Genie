/* eslint-disable no-unused-vars */
import utils from '../../modules/utils.js';

export default {
    name: 'test',
    aliases: ['lb'],
    dev: true,
    execute: async(message) => {
        // console.log(i);
    },
};
