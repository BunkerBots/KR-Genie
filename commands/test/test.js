/* eslint-disable no-unused-vars */
const utils = require('../../modules/utils');
module.exports = {
    name: 'test',
    aliases: ['lb'],
    dev: true,
    execute: async(message) => {
        // console.log(i);
    },
};
