/* eslint-disable no-unused-vars */
import utils from '../../modules/utils';

module.exports = {
    name: 'test',
    aliases: ['lb'],
    dev: true,
    execute: async(message) => {
        // console.log(i);
    },
};
